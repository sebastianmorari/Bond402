openapi: 3.1.0
info:
  title: Bond402 API
  version: 0.2.0
  description: |
    Öffentliche Bond402-API zur Prüfung eigener API-Dienste.
    Die Browser-Anwendung verwendet /api über den Vercel-Proxy.
    Für direkte Server-zu-Server-Aufrufe steht die Render-Produktionsbasis ebenfalls zur Verfügung.
servers:
  - url: /api
    description: Öffentliche Basis über die Bond402-Webanwendung (empfohlen)
  - url: https://bond402.com/api
    description: Öffentliche Produktionsbasis für Agenten und Integrationen
  - url: https://bond402-api.onrender.com/api
    description: Direkte Produktionsbasis des Bond402-API-Servers
tags:
  - name: health
  - name: auth
  - name: services
  - name: dashboard
  - name: usage
  - name: developer
  - name: api-keys
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Liveness check
      description: Confirms that the Bond402 API process is running. It does not check external dependencies.
      responses:
        "200":
          description: API process is running
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /readyz:
    get:
      operationId: readinessCheck
      tags: [health]
      summary: Readiness check
      description: Confirms that the API process can reach its PostgreSQL database.
      responses:
        "200":
          description: API is ready to serve requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"
        "503":
          description: API process is running but its database is not ready
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"
  /auth/me:
    get:
      operationId: getAuthMe
      tags: [auth]
      summary: Read the current local session user
      security:
        - Bond402Session: []
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "401":
          description: Not signed in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/register:
    post:
      operationId: registerAuthUser
      tags: [auth]
      summary: Register a local Bond402 account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthRegisterBody"
      responses:
        "201":
          description: Account created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthRegistrationResponse"
        "400":
          description: Invalid registration data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Email already registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/login:
    post:
      operationId: loginAuthUser
      tags: [auth]
      summary: Sign in to a local Bond402 account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthLoginBody"
      responses:
        "200":
          description: Signed-in user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthSession"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many login attempts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/logout:
    post:
      operationId: logoutAuthUser
      tags: [auth]
      summary: End the current local session
      security:
        - Bond402Session: []
      responses:
        "204":
          description: Signed out
  /auth/password:
    put:
      operationId: changeAuthPassword
      tags: [auth]
      summary: Change the password for the current local session
      security:
        - Bond402Session: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthChangePasswordBody"
      responses:
        "204":
          description: Password changed and all previous sessions revoked
        "400":
          description: Invalid password data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/verify-email:
    post:
      operationId: verifyAuthEmail
      tags: [auth]
      summary: Verify an email address with a one-time token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthVerifyEmailBody"
      responses:
        "200":
          description: Email verified
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthMessage"
        "400":
          description: Token is invalid, expired, or already used
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/resend-verification:
    post:
      operationId: resendAuthVerification
      tags: [auth]
      summary: Send an email verification link
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthEmailBody"
      responses:
        "202":
          description: Generic response that does not reveal account existence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthMessage"
        "429":
          description: Too many verification requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/password/forgot:
    post:
      operationId: requestAuthPasswordReset
      tags: [auth]
      summary: Request a password reset email
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthEmailBody"
      responses:
        "202":
          description: Generic response that does not reveal account existence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthMessage"
        "429":
          description: Too many password reset requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /auth/password/reset:
    post:
      operationId: resetAuthPassword
      tags: [auth]
      summary: Set a new password with a one-time reset token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthResetPasswordBody"
      responses:
        "204":
          description: Password changed and all sessions revoked
        "400":
          description: Token is invalid, expired, or already used
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not signed in or current password is invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many password change attempts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services:
    get:
      operationId: listServices
      tags: [services]
      summary: List all registered API services with their checks
      security:
        - Bond402Session: []
      responses:
        "200":
          description: Registered services
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApiService"
        "401":
          description: Not signed in
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: Database temporarily unavailable
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createService
      tags: [services]
      summary: Register an API service
      security:
        - Bond402Session: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiServiceInput"
      responses:
        "201":
          description: Created service
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiService"
        "400":
          description: Invalid service data
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          description: Not signed in
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Service already exists
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: Database temporarily unavailable
          headers:
            X-Request-ID:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services/{id}:
    get:
      operationId: getService
      tags: [services]
      summary: Get one service and its check history
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Service details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiService"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteService
      tags: [services]
      summary: Delete a registered service and its checks
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "204":
          description: Deleted
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateService
      tags: [services]
      summary: Update a registered service owned by the current user
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiServiceUpdate"
      responses:
        "200":
          description: Updated service
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiService"
        "400":
          description: Invalid update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services/{id}/checks:
    post:
      operationId: runServiceCheck
      tags: [services]
      summary: Run a safe external verification
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "201":
          description: Completed check
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckResult"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services/{id}/verify:
    post:
      operationId: verifyServiceResponse
      tags: [services]
      summary: Compare a supplied response with the expected structure
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerificationInput"
      responses:
        "201":
          description: Verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CheckResult"
        "400":
          description: Invalid actual response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services/{id}/domain-verification:
    post:
      operationId: issueDomainVerification
      tags: [services]
      summary: Issue a one-time domain verification token
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Verification instructions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainVerificationIssue"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /services/{id}/domain-verification/check:
    post:
      operationId: checkDomainVerification
      tags: [services]
      summary: Check the HTTPS well-known domain verification file
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Domain verification status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainVerificationResult"
        "400":
          description: Verification has not been started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      summary: Get Bond402 dashboard metrics
      security:
        - Bond402Session: []
      responses:
        "200":
          description: Dashboard metrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardMetrics"
  /usage:
    get:
      operationId: getUsage
      tags: [usage]
      summary: Read the current monthly check quota
      security:
        - Bond402Session: []
      responses:
        "200":
          description: Current plan and monthly usage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsageSummary"
        "401":
          description: Not signed in
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api-keys:
    get:
      operationId: listApiKeys
      tags: [api-keys]
      summary: List API key metadata for the signed-in user
      security:
        - Bond402Session: []
      responses:
        "200":
          description: API keys
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApiKey"
    post:
      operationId: createApiKey
      tags: [api-keys]
      summary: Create an API key and reveal its secret once
      security:
        - Bond402Session: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApiKeyInput"
      responses:
        "201":
          description: Created API key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreatedApiKey"
        "400":
          description: Invalid name or key limit reached
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many key-management requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api-keys/{id}:
    delete:
      operationId: revokeApiKey
      tags: [api-keys]
      summary: Revoke an API key owned by the signed-in user
      security:
        - Bond402Session: []
      parameters:
        - $ref: "#/components/parameters/ApiKeyId"
      responses:
        "204":
          description: Revoked
        "404":
          description: API key not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many key-management requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /developer/services/{id}:
    get:
      operationId: developerGetService
      tags: [developer]
      summary: Read one owned service using a Bond402 API key
      security:
        - Bond402ApiKey: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Owned service with trust score and recent check history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeveloperServiceResult"
        "401":
          description: Missing, invalid, or revoked API key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /developer/services/{id}/checks:
    post:
      operationId: developerRunServiceCheck
      tags: [developer]
      summary: Run a live check for one owned service using a Bond402 API key
      security:
        - Bond402ApiKey: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "201":
          description: Completed check with updated trust score and recent history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeveloperServiceResult"
        "401":
          description: Missing, invalid, or revoked API key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /developer/services/{id}/checks/latest:
    get:
      operationId: developerGetLatestCheck
      tags: [developer]
      summary: Read the latest check and trust score for one owned service
      security:
        - Bond402ApiKey: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Latest check, trust score, and recent history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeveloperServiceResult"
        "401":
          description: Missing, invalid, or revoked API key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service or check not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /developer/services/{id}/pre-action-check:
    post:
      operationId: developerPreActionCheck
      tags: [developer]
      summary: Decide whether an AI agent should use an owned service
      description: |
        Returns ALLOW, CAUTION, or BLOCK using the latest reachability,
        latency, structure, PASS/FAIL history, Trust Score, freshness, and
        detected anomalies. One product check is consumed from the monthly quota.
      security:
        - Bond402ApiKey: []
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Machine-readable pre-action decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeveloperPreActionCheck"
        "401":
          description: Missing, invalid, or revoked API key
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limit or monthly quota exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/discovery:
    get:
      operationId: getPublicDiscovery
      tags: [developer]
      summary: Discover public Bond402 agent endpoints
      responses:
        "200":
          description: Public discovery metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicDiscovery"
        "429":
          description: Public discovery rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/services:
    get:
      operationId: searchPublicServices
      tags: [developer]
      summary: Search the public catalog of operator-listed services
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
            maxLength: 120
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: pageSize
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Public catalog page
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicServiceCatalog"
        "400":
          description: Invalid search or pagination input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Public catalog rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/services/{id}:
    get:
      operationId: getPublicService
      tags: [developer]
      summary: Read public trust metadata or external discovery detail
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Public service metadata
          content:
            application/json:
              schema:
                 oneOf:
                   - $ref: "#/components/schemas/PublicService"
                   - $ref: "#/components/schemas/PublicExternalDiscoveryDetail"
        "404":
          description: Service is private, unlisted, deleted, or unknown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Public catalog rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/services/{id}/pre-action-check:
    get:
      operationId: getPublicPreActionCheck
      tags: [developer]
      summary: Get a read-only decision for a listed service before an action
      description: |
        Returns ALLOW, CAUTION, or BLOCK from stored checks for an operator-listed
        service. This public read-only result does not run a live check and does not
        consume the owner's monthly product quota. Live checks and owner actions still
        require a Developer API key.
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      responses:
        "200":
          description: Public pre-action decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicPreActionCheck"
        "400":
          description: Invalid action context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service is private, unlisted, deleted, or unknown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Public catalog rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: postPublicPreActionCheck
      tags: [developer]
      summary: Get a public pre-action decision with explicit action context
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicPreActionBody"
      responses:
        "200":
          description: Public pre-action decision
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicPreActionCheck"
        "400":
          description: Invalid action context
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Service is private, unlisted, deleted, or unknown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Public catalog rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/services/{id}/external-check:
    post:
      operationId: postPublicExternalCheck
      tags: [developer]
      summary: Run one safe, read-only check for an external discovery candidate
      description: |
        Runs a rate-limited Bond402 check only for the exact HTTPS GET/HEAD endpoint
        that Bond402 derived from the external OpenAPI document. No credentials are
        sent, the result is not persisted, and the endpoint remains unverified external.
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicExternalCheckBody"
      responses:
        "200":
          description: Non-persisted Bond402 observation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicExternalCheckResponse"
        "400":
          description: Endpoint is not an approved safe candidate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: External discovery record is unknown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: External check rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /public/services/{id}/external-preflight:
    post:
      operationId: postPublicExternalPreflight
      tags: [developer]
      summary: Run a safe preflight for an external API server
      description: |
        Runs a rate-limited, non-authenticated HEAD preflight against an exact
        HTTPS server declared by the external OpenAPI document. No API token is
        invented or sent, no function endpoint is called, and the result is not persisted.
      parameters:
        - $ref: "#/components/parameters/ServiceId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PublicExternalPreflightBody"
      responses:
        "200":
          description: Non-persisted passive or HEAD preflight observation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicExternalPreflightResponse"
        "404":
          description: External discovery record is unknown
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: External preflight rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /openapi.json:
    get:
      operationId: getPublicOpenApi
      tags: [developer]
      summary: Download the public Bond402 OpenAPI document
      responses:
        "200":
          description: OpenAPI 3.1 JSON document
        "429":
          description: Public catalog rate limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /demo-services:
    get:
      operationId: listDemoServices
      tags: [services]
      summary: List safe public demo endpoints
      responses:
        "200":
          description: Demo service templates
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DemoService"
components:
  securitySchemes:
    Bond402ApiKey:
      type: http
      scheme: bearer
      bearerFormat: Bond402 API key
      description: "API-Schlüssel aus dem Developer-Bereich. Nur als Authorization: Bearer b402_… senden und niemals in URLs, Logs oder Frontend-Code eintragen."
    Bond402Session:
      type: apiKey
      in: cookie
      name: bond402_session
      description: HttpOnly-Session-Cookie der lokalen Web-Anmeldung. Nicht für die Developer-API verwenden.
  parameters:
    ServiceId:
      name: id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
    ApiKeyId:
      name: id
      in: path
      required: true
      schema:
        type: string
        minLength: 1
  headers:
    RequestId:
      description: Correlation ID for this request, also present in server logs.
      schema:
        type: string
        minLength: 1
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]
    ReadinessStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ok, unavailable]
      required: [status]
    AuthRegisterBody:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 80
        email:
          type: string
          maxLength: 320
          pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$"
        password:
          type: string
          minLength: 8
          maxLength: 128
      required: [name, email, password]
    AuthLoginBody:
      type: object
      properties:
        email:
          type: string
          maxLength: 320
          pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$"
        password:
          type: string
          minLength: 8
          maxLength: 128
      required: [email, password]
    AuthChangePasswordBody:
      type: object
      properties:
        currentPassword:
          type: string
          minLength: 8
          maxLength: 128
        newPassword:
          type: string
          minLength: 8
          maxLength: 128
      required: [currentPassword, newPassword]
    AuthEmailBody:
      type: object
      properties:
        email:
          type: string
          maxLength: 320
          pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$"
      required: [email]
    AuthVerifyEmailBody:
      type: object
      properties:
        token:
          type: string
          minLength: 32
          maxLength: 200
      required: [token]
    AuthResetPasswordBody:
      type: object
      properties:
        token:
          type: string
          minLength: 32
          maxLength: 200
        newPassword:
          type: string
          minLength: 8
          maxLength: 128
      required: [token, newPassword]
    AuthMessage:
      type: object
      properties:
        message:
          type: string
      required: [message]
    AuthUser:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
          pattern: "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$"
        name:
          type: string
        createdAt:
          type: string
          format: date-time
        emailVerified:
          type: boolean
      required: [id, email, name, createdAt, emailVerified]
    AuthSession:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/AuthUser"
      required: [user]
    AuthRegistrationResponse:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/AuthUser"
        message:
          type: string
        verificationRequired:
          type: boolean
      required: [user, message, verificationRequired]
    ApiError:
      type: object
      properties:
        error:
          type: string
        code:
          type: string
        quota:
          anyOf:
            - $ref: "#/components/schemas/UsageSummary"
            - type: "null"
      required: [error, code]
    ApiKeyInput:
      type: object
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 80
      required: [name]
    ApiKey:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        prefix:
          type: string
        createdAt:
          type: string
          format: date-time
        lastUsedAt:
          type: ["string", "null"]
          format: date-time
        revokedAt:
          type: ["string", "null"]
          format: date-time
      required: [id, name, prefix, createdAt, lastUsedAt, revokedAt]
    CreatedApiKey:
      allOf:
        - $ref: "#/components/schemas/ApiKey"
        - type: object
          properties:
            secret:
              type: string
          required: [secret]
    DeveloperService:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        url:
          type: string
        expectedStructure:
          type: string
        responseMode:
          $ref: "#/components/schemas/ServiceResponseMode"
        maxResponseTime:
          type: number
      required: [id, name, url, expectedStructure, responseMode, maxResponseTime]
    ServiceRequestMethod:
      type: string
      enum: [GET, HEAD, POST]
    ServiceResponseMode:
      type: string
      enum: [JSON, HTTP]
    TargetAuthType:
      type: string
      enum: [NONE, BEARER, API_KEY_HEADER]
    ServiceSourceType:
      type: string
      enum: [MANUAL, EXTERNAL_DISCOVERY]
    AuthRequirement:
      type: string
      enum: [REQUIRED, NOT_REQUIRED, NOT_DECLARED, UNKNOWN]
    DomainRelationship:
      type: string
      enum: [OWNED, THIRD_PARTY]
    DeveloperServiceResult:
      type: object
      properties:
        service:
          $ref: "#/components/schemas/DeveloperService"
        trustScore:
          type: ["number", "null"]
        trustExplanation:
          type: string
        availabilityScore:
          type: ["number", "null"]
        securityConfidence:
          $ref: "#/components/schemas/SecurityConfidence"
        latestCheck:
          anyOf:
            - $ref: "#/components/schemas/CheckResult"
            - type: "null"
        checks:
          type: array
          maxItems: 100
          items:
            $ref: "#/components/schemas/CheckResult"
      required: [service, trustScore, trustExplanation, availabilityScore, securityConfidence, latestCheck, checks]
    ApiServiceInput:
      type: object
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 100
        url:
          type: string
          maxLength: 2048
          pattern: "^https?://"
        sourceType:
          $ref: "#/components/schemas/ServiceSourceType"
          default: MANUAL
        sourceProvider:
          type: string
          maxLength: 200
        sourceUrl:
          type: string
          maxLength: 2048
          pattern: "^https?://"
        authRequirement:
          $ref: "#/components/schemas/AuthRequirement"
          default: UNKNOWN
        discoveryMetadata:
          type: object
          additionalProperties: true
          maxProperties: 30
        expectedStructure:
          type: string
          maxLength: 4000
        responseMode:
          $ref: "#/components/schemas/ServiceResponseMode"
          default: JSON
        maxResponseTime:
          type: number
          minimum: 100
          maximum: 15000
        requestMethod:
          $ref: "#/components/schemas/ServiceRequestMethod"
          default: GET
        targetAuthType:
          $ref: "#/components/schemas/TargetAuthType"
          default: NONE
        targetAuthHeaderName:
          type: string
          minLength: 1
          maxLength: 128
          pattern: "^[A-Za-z0-9][A-Za-z0-9-]*[A-Za-z0-9]$"
        targetAuthSecret:
          type: string
          minLength: 1
          maxLength: 4096
          writeOnly: true
        requestBody:
          type: object
          additionalProperties: true
          maxProperties: 100
        domainRelationship:
          $ref: "#/components/schemas/DomainRelationship"
          default: THIRD_PARTY
      required: [name, url, maxResponseTime]
    ApiServiceUpdate:
      type: object
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 100
        url:
          type: string
          maxLength: 2048
          pattern: "^https?://"
        sourceType:
          $ref: "#/components/schemas/ServiceSourceType"
        sourceProvider:
          type: ["string", "null"]
          maxLength: 200
        sourceUrl:
          type: ["string", "null"]
          maxLength: 2048
          pattern: "^https?://"
        authRequirement:
          $ref: "#/components/schemas/AuthRequirement"
        discoveryMetadata:
          type: ["object", "null"]
          additionalProperties: true
          maxProperties: 30
        expectedStructure:
          type: string
          maxLength: 4000
        responseMode:
          $ref: "#/components/schemas/ServiceResponseMode"
        maxResponseTime:
          type: number
          minimum: 100
          maximum: 15000
        visibility:
          type: string
          enum: [PRIVATE, LISTED]
        requestMethod:
          $ref: "#/components/schemas/ServiceRequestMethod"
        targetAuthType:
          $ref: "#/components/schemas/TargetAuthType"
        targetAuthHeaderName:
          type: ["string", "null"]
          maxLength: 128
          pattern: "^[A-Za-z0-9][A-Za-z0-9-]*[A-Za-z0-9]$"
        targetAuthSecret:
          type: string
          minLength: 1
          maxLength: 4096
          writeOnly: true
        requestBody:
          type: ["object", "null"]
          additionalProperties: true
          maxProperties: 100
        domainRelationship:
          $ref: "#/components/schemas/DomainRelationship"
    ApiService:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        url:
          type: string
        sourceType:
          $ref: "#/components/schemas/ServiceSourceType"
        sourceProvider:
          type: ["string", "null"]
        sourceUrl:
          type: ["string", "null"]
        authRequirement:
          $ref: "#/components/schemas/AuthRequirement"
        discoveryMetadata:
          type: ["object", "null"]
          additionalProperties: true
        expectedStructure:
          type: string
        responseMode:
          $ref: "#/components/schemas/ServiceResponseMode"
        maxResponseTime:
          type: number
        requestMethod:
          $ref: "#/components/schemas/ServiceRequestMethod"
        targetAuthType:
          $ref: "#/components/schemas/TargetAuthType"
        targetAuthHeaderName:
          type: ["string", "null"]
        targetAuthSecretConfigured:
          type: boolean
        requestBody:
          type: ["object", "null"]
          additionalProperties: true
        domainRelationship:
          $ref: "#/components/schemas/DomainRelationship"
        visibility:
          type: string
          enum: [PRIVATE, LISTED]
        listedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        securityStatus:
          $ref: "#/components/schemas/SecurityStatus"
        firstSeenAt:
          type: string
          format: date-time
        sandboxObservedAt:
          type: ["string", "null"]
          format: date-time
        trustScore:
          type: ["number", "null"]
        trustExplanation:
          type: string
        availabilityScore:
          type: ["number", "null"]
        securityConfidence:
          $ref: "#/components/schemas/SecurityConfidence"
        trustMetrics:
          $ref: "#/components/schemas/TrustMetrics"
        signals:
          $ref: "#/components/schemas/SignalStates"
        domainVerification:
          $ref: "#/components/schemas/DomainVerification"
        checks:
          type: array
          items:
            $ref: "#/components/schemas/CheckResult"
      required: [id, name, url, expectedStructure, responseMode, maxResponseTime, requestMethod, targetAuthType, targetAuthHeaderName, targetAuthSecretConfigured, requestBody, domainRelationship, visibility, listedAt, createdAt, securityStatus, firstSeenAt, sandboxObservedAt, trustScore, trustExplanation, availabilityScore, securityConfidence, trustMetrics, signals, domainVerification, checks]
    CheckResult:
      type: object
      properties:
        id:
          type: string
        serviceId:
          type: string
        checkedAt:
          type: string
          format: date-time
        status:
          type: string
          enum: [PASS, FAIL, REVIEW]
        checkType:
          type: string
          enum: [LIVE, MANUAL]
        reachable:
          type: boolean
        responseTimeMs:
          type: number
        structureMatch:
          type: boolean
        httpStatus:
          type: ["number", "null"]
        errorCode:
          type: ["string", "null"]
        summary:
          type: string
        foundFields:
          type: array
          items:
            type: string
        missingFields:
          type: array
          items:
            type: string
        https:
          type: boolean
        tlsStatus:
          $ref: "#/components/schemas/SignalState"
        tlsExpiresAt:
          type: ["string", "null"]
          format: date-time
        tlsDaysRemaining:
          type: ["number", "null"]
        securityHeaders:
          $ref: "#/components/schemas/SecurityHeaders"
        securitySignals:
          $ref: "#/components/schemas/SecuritySignals"
        probeRegion:
          type: string
      required: [id, serviceId, checkedAt, status, checkType, reachable, responseTimeMs, structureMatch, httpStatus, errorCode, summary, foundFields, missingFields, https, tlsStatus, tlsExpiresAt, tlsDaysRemaining, securityHeaders, securitySignals, probeRegion]
    VerificationInput:
      type: object
      properties:
        actualResponse:
          type: string
          minLength: 1
          maxLength: 100000
      required: [actualResponse]
    DashboardMetrics:
      type: object
      properties:
        serviceCount:
          type: number
        checkCount:
          type: number
        passRate:
          type: number
        averageResponseTimeMs:
          type: number
        uptimePercent:
          type: ["number", "null"]
        p95ResponseTimeMs:
          type: ["number", "null"]
        p99ResponseTimeMs:
          type: ["number", "null"]
        timedSampleCount:
          type: number
      required: [serviceCount, checkCount, passRate, averageResponseTimeMs, uptimePercent, p95ResponseTimeMs, p99ResponseTimeMs, timedSampleCount]
    SignalState:
      type: string
      enum: [CHECKED, WARNING, UNAVAILABLE, NOT_EVALUATED]
    DomainSignalState:
      type: string
      enum: [CHECKED, WARNING, UNAVAILABLE, NOT_EVALUATED, NOT_APPLICABLE]
    SecurityHeaders:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/SignalState"
        evaluated:
          type: array
          items:
            type: string
        present:
          type: array
          items:
            type: string
        missing:
          type: array
          items:
            type: string
      required: [status, evaluated, present, missing]
    SecuritySignalStatus:
      type: string
      enum: [PASS, WARNING, FAIL, UNKNOWN]
    SecurityStatus:
      type: string
      enum: [SANDBOX_PENDING, SANDBOXED_OBSERVED, VERIFIED_LOW_RISK, SUSPICIOUS, FLAGGED]
    ResponseContentKind:
      type: string
      enum: [JSON, TEXT, HTML, JAVASCRIPT, BINARY, DOWNLOAD, UNKNOWN]
    SecurityConfidence:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/SecuritySignalStatus"
        score:
          type: ["number", "null"]
        summary:
          type: string
      required: [status, score, summary]
    ThreatIndicators:
      type: object
      properties:
        status:
          type: string
          enum: [NONE_DETECTED, SUSPICIOUS, FLAGGED, UNKNOWN]
        severity:
          type: string
          enum: [LOW, MEDIUM, HIGH, CRITICAL]
        confidence:
          type: number
        indicators:
          type: array
          items:
            type: string
        summary:
          type: string
      required: [status, severity, confidence, indicators, summary]
    HistoricalDrift:
      type: object
      properties:
        status:
          type: string
          enum: [NONE, CHANGED, UNKNOWN]
        indicators:
          type: array
          items:
            type: string
        summary:
          type: string
      required: [status, indicators, summary]
    SecuritySignals:
      type: object
      properties:
        reachability:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
          required: [status, summary]
        transport:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            https:
              type: boolean
            protocol:
              type: ["string", "null"]
            certificateValid:
              type: ["boolean", "null"]
            expiresAt:
              type: ["string", "null"]
            daysRemaining:
              type: ["number", "null"]
          required: [status, summary, https, protocol, certificateValid, expiresAt, daysRemaining]
        network:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
          required: [status, summary]
        redirects:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            count:
              type: number
            crossOrigin:
              type: boolean
            downgraded:
              type: boolean
          required: [status, summary, count, crossOrigin, downgraded]
        responseType:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            kind:
              $ref: "#/components/schemas/ResponseContentKind"
            contentType:
              type: ["string", "null"]
          required: [status, summary, kind, contentType]
        suspiciousPayload:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            indicators:
              type: array
              items:
                type: string
          required: [status, summary, indicators]
        securityHeaders:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            evaluated:
              type: array
              items:
                type: string
            present:
              type: array
              items:
                type: string
            missing:
              type: array
              items:
                type: string
          required: [status, summary, evaluated, present, missing]
        reputation:
          type: object
          properties:
            status:
              type: string
              enum: [UNKNOWN]
            summary:
              type: string
          required: [status, summary]
        rateLimit:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            detected:
              type: boolean
            retryAfterSeconds:
              type: ["number", "null"]
          required: [status, summary, detected, retryAfterSeconds]
        authentication:
          type: object
          properties:
            status:
              $ref: "#/components/schemas/SecuritySignalStatus"
            summary:
              type: string
            required:
              type: boolean
          required: [status, summary, required]
        securityConfidence:
          $ref: "#/components/schemas/SecurityConfidence"
        securityStatus:
          $ref: "#/components/schemas/SecurityStatus"
        firstSeenAt:
          type: string
          format: date-time
        sandboxObservedAt:
          type: ["string", "null"]
          format: date-time
        threatIndicators:
          $ref: "#/components/schemas/ThreatIndicators"
        historicalDrift:
          $ref: "#/components/schemas/HistoricalDrift"
      required: [reachability, transport, network, redirects, responseType, suspiciousPayload, securityHeaders, reputation, rateLimit, authentication, securityConfidence, threatIndicators, historicalDrift]
    SignalStates:
      type: object
      properties:
        https:
          $ref: "#/components/schemas/SignalState"
        tls:
          $ref: "#/components/schemas/SignalState"
        securityHeaders:
          $ref: "#/components/schemas/SignalState"
      required: [https, tls, securityHeaders]
    TrustMetrics:
      type: object
      properties:
        regionalAggregation:
          $ref: "#/components/schemas/RegionalAggregation"
        sampleCount:
          type: number
        timedSampleCount:
          type: number
        uptimePercent:
          type: ["number", "null"]
        averageResponseTimeMs:
          type: ["number", "null"]
        p95ResponseTimeMs:
          type: ["number", "null"]
        p99ResponseTimeMs:
          type: ["number", "null"]
        withinTargetPercent:
          type: ["number", "null"]
        windowStartAt:
          type: ["string", "null"]
          format: date-time
        latestCheckAt:
          type: ["string", "null"]
          format: date-time
        weighting:
          type: object
          properties:
            method:
              type: string
            halfLifeDays:
              type: number
            description:
              type: string
          required: [method, halfLifeDays, description]
      required: [regionalAggregation, sampleCount, timedSampleCount, uptimePercent, averageResponseTimeMs, p95ResponseTimeMs, p99ResponseTimeMs, withinTargetPercent, windowStartAt, latestCheckAt, weighting]
    RegionalAggregation:
      type: object
      properties:
        state:
          type: string
          enum: [SINGLE_REGION, MULTIPLE_REGIONS, CONTRADICTORY_REGIONAL_RESULTS, INSUFFICIENT_REGIONAL_DATA]
        regionCount:
          type: number
        regions:
          type: array
          items:
            type: string
        liveCheckCount:
          type: number
        evaluatedCheckCount:
          type: number
        unassignedLiveCheckCount:
          type: number
        regionalResults:
          type: array
          items:
            type: object
            properties:
              region:
                type: string
              sampleCount:
                type: number
              latestCheckAt:
                type: string
                format: date-time
              latestStatus:
                type: string
                enum: [PASS, FAIL, REVIEW]
              latestReachable:
                type: boolean
            required: [region, sampleCount, latestCheckAt, latestStatus, latestReachable]
        contradictorySignals:
          type: array
          items:
            type: string
            enum: [status, reachability, https, tls, securityHeaders]
        observationBasis:
          type: string
          enum: [STORED_LIVE_CHECKS]
        continuousMonitoring:
          type: boolean
          enum: [false]
        description:
          type: string
      required: [state, regionCount, regions, liveCheckCount, evaluatedCheckCount, unassignedLiveCheckCount, regionalResults, contradictorySignals, observationBasis, continuousMonitoring, description]
    DomainVerification:
      type: object
      properties:
        status:
          type: string
          enum: [VERIFIED, PENDING, NOT_STARTED, NOT_APPLICABLE, NOT_EVALUATED]
        verifiedAt:
          type: ["string", "null"]
          format: date-time
      required: [status, verifiedAt]
    DomainVerificationIssue:
      type: object
      properties:
        status:
          type: string
          enum: [PENDING]
        token:
          type: string
        path:
          type: string
        instructions:
          type: string
        issuedAt:
          type: string
          format: date-time
      required: [status, token, path, instructions, issuedAt]
    DomainVerificationResult:
      type: object
      properties:
        status:
          type: string
          enum: [VERIFIED, PENDING]
        verifiedAt:
          type: ["string", "null"]
          format: date-time
        reason:
          type: string
      required: [status, verifiedAt, reason]
    UsageSummary:
      type: object
      properties:
        plan:
          type: string
          enum: [FREE, STARTER, PRO, BUSINESS, ENTERPRISE]
        planName:
          type: string
        monthlyLimit:
          type: ["number", "null"]
        usedChecks:
          type: number
        remainingChecks:
          type: ["number", "null"]
        periodStart:
          type: string
          format: date-time
        resetAt:
          type: string
          format: date-time
        upgradeAvailable:
          type: boolean
        priceChf:
          type: ["number", "null"]
        description:
          type: string
        availablePlans:
          type: array
          items:
            $ref: "#/components/schemas/PlanOption"
      required: [plan, planName, monthlyLimit, usedChecks, remainingChecks, periodStart, resetAt, upgradeAvailable, priceChf, description, availablePlans]
    PlanOption:
      type: object
      properties:
        plan:
          type: string
          enum: [FREE, STARTER, PRO, BUSINESS, ENTERPRISE]
        planName:
          type: string
        monthlyLimit:
          type: ["number", "null"]
        priceChf:
          type: ["number", "null"]
        description:
          type: string
      required: [plan, planName, monthlyLimit, priceChf, description]
    DeveloperPreActionFactors:
      type: object
      properties:
        trustScore:
          type: ["number", "null"]
        latestStatus:
          type: ["string", "null"]
          enum: [PASS, FAIL, REVIEW, null]
        latestCheckAt:
          type: ["string", "null"]
          format: date-time
        latestResponseTimeMs:
          type: ["number", "null"]
        latestReachable:
          type: ["boolean", "null"]
        latestStructureMatch:
          type: ["boolean", "null"]
        signals:
          type: object
          properties:
            https:
              $ref: "#/components/schemas/SignalState"
            tls:
              $ref: "#/components/schemas/SignalState"
            securityHeaders:
              $ref: "#/components/schemas/SignalState"
            domain:
              $ref: "#/components/schemas/DomainSignalState"
          required: [https, tls, securityHeaders, domain]
        trustMetrics:
          $ref: "#/components/schemas/TrustMetrics"
        recentLiveChecks:
          type: number
        recentPasses:
          type: number
        recentFailures:
          type: number
        anomalies:
          type: array
          items:
            type: string
      required: [trustScore, latestStatus, latestCheckAt, latestResponseTimeMs, latestReachable, latestStructureMatch, signals, trustMetrics, recentLiveChecks, recentPasses, recentFailures, anomalies]
    DeveloperPreActionCheck:
      type: object
      properties:
        serviceId:
          type: string
        serviceName:
          type: string
        decision:
          type: string
          enum: [ALLOW, CAUTION, BLOCK]
        reasons:
          type: array
          items:
            type: string
        factors:
          $ref: "#/components/schemas/DeveloperPreActionFactors"
        actionContext:
          $ref: "#/components/schemas/ActionContext"
        freshness:
          $ref: "#/components/schemas/Freshness"
        policy:
          $ref: "#/components/schemas/PreActionPolicy"
        evaluatedAt:
          type: string
          format: date-time
      required: [serviceId, serviceName, decision, reasons, factors, actionContext, freshness, policy, evaluatedAt]
    ActionContext:
      type: string
      enum: [GENERAL, READ, WRITE, PAYMENT, CREDENTIAL_USE]
    Freshness:
      type: object
      properties:
        state:
          type: string
          enum: [FRESH, STALE, UNKNOWN]
        ageSeconds:
          type: ["number", "null"]
        maxAgeSeconds:
          type: number
      required: [state, ageSeconds, maxAgeSeconds]
    PreActionPolicy:
      type: object
      properties:
        id:
          type: string
        version:
          type: string
        maxFreshnessSeconds:
          type: number
      required: [id, version, maxFreshnessSeconds]
    PublicService:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        url:
          type: string
        visibility:
          type: string
          enum: [LISTED]
        listedAt:
          type: ["string", "null"]
          format: date-time
        trustScore:
          type: ["number", "null"]
        trustExplanation:
          type: string
        availabilityScore:
          type: ["number", "null"]
        securityConfidence:
          $ref: "#/components/schemas/SecurityConfidence"
        trustMetrics:
          $ref: "#/components/schemas/TrustMetrics"
        signals:
          $ref: "#/components/schemas/SignalStates"
        domainVerification:
          $ref: "#/components/schemas/DomainVerification"
        domainRelationship:
          $ref: "#/components/schemas/DomainRelationship"
        latestStatus:
          type: ["string", "null"]
          enum: [PASS, FAIL, REVIEW, null]
        latestCheckAt:
          type: ["string", "null"]
          format: date-time
        latestCheck:
          anyOf:
            - type: object
              properties:
                checkedAt:
                  type: string
                  format: date-time
                status:
                  type: string
                  enum: [PASS, FAIL, REVIEW]
                reachable:
                  type: boolean
                responseTimeMs:
                  type: number
                structureMatch:
                  type: boolean
                httpStatus:
                  type: ["number", "null"]
                https:
                  type: boolean
                tlsStatus:
                  $ref: "#/components/schemas/SignalState"
                tlsExpiresAt:
                  type: ["string", "null"]
                  format: date-time
                tlsDaysRemaining:
                  type: ["number", "null"]
                securityHeaders:
                  $ref: "#/components/schemas/SecurityHeaders"
                securitySignals:
                  $ref: "#/components/schemas/SecuritySignals"
                probeRegion:
                  type: string
              required: [checkedAt, status, reachable, responseTimeMs, structureMatch, httpStatus, https, tlsStatus, tlsExpiresAt, tlsDaysRemaining, securityHeaders, securitySignals, probeRegion]
            - type: "null"
        access:
          type: object
          properties:
            publicCatalog:
              type: boolean
            preActionRequiresDeveloperKey:
              type: boolean
            liveCheckRequiresDeveloperKey:
              type: boolean
          required: [publicCatalog, preActionRequiresDeveloperKey, liveCheckRequiresDeveloperKey]
        links:
          type: object
          properties:
            detail:
              type: string
            preActionCheck:
              type: string
            privilegedPreActionCheck:
              type: string
          required: [detail, preActionCheck, privilegedPreActionCheck]
      required: [id, name, url, visibility, listedAt, securityStatus, firstSeenAt, sandboxObservedAt, trustScore, trustExplanation, availabilityScore, securityConfidence, trustMetrics, signals, domainVerification, domainRelationship, latestStatus, latestCheckAt, latestCheck, access, links]
    PublicServiceCatalog:
      type: object
      properties:
        items:
          type: array
          items:
            oneOf:
              - $ref: "#/components/schemas/PublicServiceSearchResult"
              - $ref: "#/components/schemas/PublicExternalService"
        query:
          type: string
        page:
          type: number
        pageSize:
          type: number
        total:
          type: number
        hasNextPage:
          type: boolean
        sort:
          type: string
        source:
          $ref: "#/components/schemas/PublicDiscoverySource"
      required: [items, query, page, pageSize, total, hasNextPage, sort, source]
    PublicDiscoverySource:
      type: object
      properties:
        source:
          type: string
          enum: [BOND402_INTERNAL_CATALOG, APIS_GURU_OPENAPI_DIRECTORY, PUBLIC_EXTERNAL_CATALOG]
        sourceLabel:
          type: string
        scope:
          type: string
          enum: [LISTED_SERVICES_ONLY, PUBLIC_UNVERIFIED_OPENAPI, PUBLIC_UNVERIFIED_API_DIRECTORY, PUBLIC_UNVERIFIED_EXTERNAL_CATALOG]
        externalSources:
          type: boolean
        mode:
          type: string
          enum: [INTERNAL_PRIMARY, EXTERNAL_FALLBACK]
        fallback:
          type: string
          enum: [NOT_USED, USED, UNAVAILABLE]
        sourceUrl:
          type: string
      required: [source, sourceLabel, scope, externalSources, mode, fallback]
    PublicServiceDiscovery:
      type: object
      properties:
        source:
          type: string
          enum: [BOND402_INTERNAL_CATALOG]
        sourceLabel:
          type: string
        scope:
          type: string
          enum: [LISTED_SERVICES_ONLY]
        matchScore:
          type: number
          minimum: 0
          maximum: 100
        rankingFactors:
          type: object
          properties:
            textRelevance:
              type: number
              minimum: 0
              maximum: 1
            observationCoverage:
              type: number
              minimum: 0
              maximum: 1
            observationFreshness:
              type: number
              minimum: 0
              maximum: 1
            publicSource:
              type: number
              minimum: 0
              maximum: 1
          required: [textRelevance, observationCoverage, observationFreshness, publicSource]
        evidence:
          type: object
          properties:
            liveObservationCount:
              type: number
              minimum: 0
            latestObservationAt:
              type: ["string", "null"]
              format: date-time
            publicSourceUrl:
              type: string
          required: [liveObservationCount, latestObservationAt, publicSourceUrl]
      required: [source, sourceLabel, scope, matchScore, rankingFactors, evidence]
    PublicServiceSearchResult:
      allOf:
        - $ref: "#/components/schemas/PublicService"
        - type: object
          properties:
            discovery:
              $ref: "#/components/schemas/PublicServiceDiscovery"
          required: [discovery]
    PublicExternalDiscovery:
      type: object
      properties:
        source:
          type: string
          enum: [APIS_GURU_OPENAPI_DIRECTORY, PUBLIC_APIS_DIRECTORY]
        sourceLabel:
          type: string
        scope:
          type: string
          enum: [PUBLIC_UNVERIFIED_OPENAPI, PUBLIC_UNVERIFIED_API_DIRECTORY]
        verification:
          type: string
          enum: [UNVERIFIED_EXTERNAL]
        matchScore:
          type: number
          minimum: 0
          maximum: 100
        rankingFactors:
          type: object
          properties:
            textRelevance:
              type: number
              minimum: 0
              maximum: 1
            sourceFreshness:
              type: number
              minimum: 0
              maximum: 1
            openApiMetadata:
              type: number
              minimum: 0
              maximum: 1
          required: [textRelevance, sourceFreshness, openApiMetadata]
        evidence:
          type: object
          properties:
            provider:
              type: string
            sourceRecordUrl:
              type: string
            specificationUrl:
              type: string
            openapiVersion:
              type: ["string", "null"]
            updatedAt:
              type: ["string", "null"]
              format: date-time
          required: [provider, sourceRecordUrl, specificationUrl, openapiVersion, updatedAt]
      required: [source, sourceLabel, scope, verification, matchScore, rankingFactors, evidence]
    PublicExternalService:
      type: object
      properties:
        id:
          type: string
        kind:
          type: string
          enum: [EXTERNAL_DISCOVERY]
        name:
          type: string
        description:
          type: ["string", "null"]
        url:
          type: string
        verification:
          type: object
          properties:
            status:
              type: string
              enum: [UNVERIFIED_EXTERNAL]
            reason:
              type: string
              enum: [SOURCE_METADATA_ONLY_NO_BOND402_CHECK]
          required: [status, reason]
        discovery:
          $ref: "#/components/schemas/PublicExternalDiscovery"
        links:
          type: object
          properties:
            sourceRecord:
              type: string
            specification:
              type: string
          required: [sourceRecord, specification]
      required: [id, kind, name, description, url, verification, discovery, links]
    PublicExternalDiscoveryDetail:
      type: object
      properties:
        id:
          type: string
        kind:
          type: string
          enum: [EXTERNAL_DISCOVERY_DETAIL]
        name:
          type: string
        provider:
          type: string
        version:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        source:
          type: object
          properties:
            id:
              type: string
              enum: [APIS_GURU_OPENAPI_DIRECTORY, PUBLIC_APIS_DIRECTORY]
            label:
              type: string
            catalogUrl:
              type: string
              format: uri
            recordUrl:
              type: string
              format: uri
            specificationUrl:
              type: string
              format: uri
          required: [id, label, catalogUrl, recordUrl, specificationUrl]
        verification:
          type: object
          properties:
            status:
              type: string
              enum: [UNVERIFIED_EXTERNAL]
            reason:
              type: string
              enum: [SPECIFICATION_METADATA_ONLY_NO_BOND402_CHECK]
          required: [status, reason]
        specification:
          type: object
          properties:
            status:
              type: string
              enum: [PARSED, UNAVAILABLE, UNSUPPORTED]
            openapiVersion:
              type: ["string", "null"]
            title:
              type: ["string", "null"]
            description:
              type: ["string", "null"]
            servers:
              type: array
              items:
                type: object
                properties:
                  url:
                    type: string
                    format: uri
                  description:
                    type: ["string", "null"]
                  templated:
                    type: boolean
                required: [url, description, templated]
            auth:
              type: object
              properties:
                status:
                  type: string
                  enum: [REQUIRED, NOT_REQUIRED, NOT_DECLARED, UNKNOWN]
                schemes:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      type:
                        type: string
                      scheme:
                        type: ["string", "null"]
                      location:
                        type: ["string", "null"]
                    required: [name, type, scheme, location]
              required: [status, schemes]
            endpoints:
              type: array
              items:
                type: object
                properties:
                  method:
                    type: string
                  path:
                    type: string
                  summary:
                    type: ["string", "null"]
                  operationId:
                    type: ["string", "null"]
                  auth:
                    type: string
                    enum: [REQUIRED, NOT_REQUIRED, NOT_DECLARED, UNKNOWN]
                  safeToProbe:
                    type: boolean
                  reason:
                    type: string
                required: [method, path, summary, operationId, auth, safeToProbe, reason]
          required: [status, openapiVersion, title, description, servers, auth, endpoints]
        safeEndpoint:
          oneOf:
            - type: "null"
            - type: object
              properties:
                method:
                  type: string
                  enum: [GET, HEAD]
                path:
                  type: string
                url:
                  type: string
                  format: uri
                reason:
                  type: string
                  enum: [EXPLICITLY_PUBLIC_PARAMETER_FREE_READ]
              required: [method, path, url, reason]
        safeEndpoints:
          type: array
          items:
            type: object
            properties:
              method:
                type: string
                enum: [GET, HEAD]
              path:
                type: string
              url:
                type: string
                format: uri
              reason:
                type: string
                enum: [EXPLICITLY_PUBLIC_PARAMETER_FREE_READ]
            required: [method, path, url, reason]
        safeEndpointNote:
          type: string
      required: [id, kind, name, provider, version, description, source, verification, specification, safeEndpoint, safeEndpoints, safeEndpointNote]
    PublicDiscovery:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        version:
          type: string
        disclaimer:
          type: string
        authentication:
          type: object
          properties:
            publicCatalog:
              type: string
            publicPreActionCheck:
              type: string
            developerApi:
              type: string
            privilegedActions:
              type: string
          required: [publicCatalog, publicPreActionCheck, developerApi, privilegedActions]
        endpoints:
          type: object
          properties:
            catalog:
              type: string
            serviceDetail:
              type: string
            publicPreActionCheck:
              type: string
            openapi:
              type: string
            humanDocs:
              type: string
            wellKnown:
              type: string
            llms:
              type: string
          required: [catalog, serviceDetail, publicPreActionCheck, openapi, humanDocs, wellKnown, llms]
        publicResponseFields:
          type: array
          items:
            type: string
        limits:
          type: object
          properties:
            publicCatalogRequestsPerMinutePerIp:
              type: number
            publicPreActionCountsAgainstMonthlyDeveloperQuota:
              type: boolean
            developerPreActionRequiresKey:
              type: boolean
          required: [publicCatalogRequestsPerMinutePerIp, publicPreActionCountsAgainstMonthlyDeveloperQuota, developerPreActionRequiresKey]
        dataSource:
          type: object
          properties:
            source:
              type: string
              enum: [BOND402_INTERNAL_CATALOG]
            sourceLabel:
              type: string
            scope:
              type: string
              enum: [LISTED_SERVICES_ONLY]
            externalSources:
              type: boolean
              enum: [false]
            fallbackPolicy:
              type: string
            fallbacks:
              type: array
              items:
                $ref: "#/components/schemas/PublicDiscoverySource"
            ranking:
              type: array
              items:
                type: string
          required: [source, sourceLabel, scope, externalSources, fallbackPolicy, fallbacks, ranking]
        policy:
          $ref: "#/components/schemas/PreActionPolicy"
      required: [name, description, version, disclaimer, authentication, endpoints, dataSource, publicResponseFields, limits, policy]
    PublicPreActionCheck:
      allOf:
        - $ref: "#/components/schemas/DeveloperPreActionCheck"
        - type: object
          properties:
            actionContext:
              $ref: "#/components/schemas/ActionContext"
            freshness:
              $ref: "#/components/schemas/Freshness"
            policy:
              $ref: "#/components/schemas/PreActionPolicy"
            access:
              type: object
              properties:
                requiresDeveloperKey:
                  type: boolean
                liveCheckRequiresDeveloperKey:
                  type: boolean
              required: [requiresDeveloperKey, liveCheckRequiresDeveloperKey]
            usage:
              type: object
              properties:
                countsAgainstMonthlyPlan:
                  type: boolean
                rateLimit:
                  type: string
              required: [countsAgainstMonthlyPlan, rateLimit]
    PublicPreActionBody:
      type: object
      properties:
        actionContext:
          $ref: "#/components/schemas/ActionContext"
    PublicExternalCheckBody:
      type: object
      properties:
        method:
          type: string
          enum: [GET, HEAD]
        path:
          type: string
        url:
          type: string
          format: uri
      required: [method, path, url]
    PublicExternalCheckResponse:
      type: object
      properties:
        serviceId:
          type: string
        endpoint:
          $ref: "#/components/schemas/PublicExternalCheckBody"
        verification:
          type: object
          properties:
            status:
              type: string
              enum: [CHECKED_EXTERNAL]
            trustStatus:
              type: string
              enum: [UNVERIFIED_EXTERNAL]
            checkedAt:
              type: string
              format: date-time
            persisted:
              type: boolean
          required: [status, trustStatus, checkedAt, persisted]
        check:
          type: object
          additionalProperties: true
        usage:
          type: object
          additionalProperties: true
        safety:
          type: object
          additionalProperties: true
      required: [serviceId, endpoint, verification, check, usage, safety]
    PublicExternalPreflightBody:
      type: object
      properties:
        serverUrl:
          type: string
          format: uri
    PublicExternalPreflightResponse:
      type: object
      properties:
        serviceId:
          type: string
        mode:
          type: string
          enum: [PREFLIGHT, PASSIVE_ONLY]
        serverUrl:
          type: ["string", "null"]
        verification:
          type: object
          additionalProperties: true
        check:
          type: object
          additionalProperties: true
        safety:
          type: object
          additionalProperties: true
        usage:
          type: object
          additionalProperties: true
      required: [serviceId, mode, serverUrl, verification, check, safety, usage]
    DemoService:
      type: object
      properties:
        name:
          type: string
        url:
          type: string
        expectedStructure:
          type: string
        maxResponseTime:
          type: number
        explanation:
          type: string
      required: [name, url, expectedStructure, maxResponseTime, explanation]